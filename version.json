{
  "version": "2.5.1",
  "notes": "Fixed self-updating for real: 'Update now' now downloads and silently runs the actual installer (the same one you'd run by hand) instead of hand-copying files, then relaunches Pinbox automatically. This is what manual reinstalls were doing correctly all along - the in-app button now does the same thing."
}
