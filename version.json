{
  "version": "2.5.0",
  "notes": "Fixed a real crash on send: SendInput's Windows struct was undersized, corrupting memory - now correctly sized. Also fixed red bleeding through from your Windows accent color on toggle switches and focus states (now always purple regardless of OS theme color), and a self-update bug that silently updated the wrong folder for portable installs."
}
